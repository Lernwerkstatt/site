{{> main/title title="Menschen" subtitle="Wir sind Die Lernwerkstatt"}}
<div class="container">
  <div class="d-flex flex-wrap justify-content-center">
    <div class="col text-center" id="team">
      {{#each team}}
      {{> about/member this}}
      {{/each}}
    </div>
  </div>
</div>
