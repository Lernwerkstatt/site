{{#section "head"}}
<title>{{{__ "common_title"}}} – {{{__ "common_title"}}}</title>
{{/section}}

<div class="home-bg">
  <div class="jumbotron-fluid">
    <div class="container py-2">
      <div class="row">
        <div class="col-sm-8">
          <h1 class="home-logo">
            <img class="home-logo-img" alt="{{{__ "common_title"}}}" src="/img/home/fulllogo.png" />
          </h1>
          <p class="home-subtitle">
            {{{__ "common_subtitle"}}}
          </p>
        </div>
        <div class="col-sm-4">
          <div class="card bg-primary text-black overflow-hidden">
            <h5 class="card-header text-center text-light">
              {{{__ "common_calendar"}}}
            </h5>
            <ul class="list-group list-group-flush overflow-auto calendar-list">
              {{#each calendar}}
              {{> home/event this}}
              {{> home/modal this}}
              {{/each}}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
    <div class="row mt-2 mb-3">
      <div class="col-sm-12">
        <div class="card-deck text-center">
          {{> home/card ourValues}}
          {{> home/cardModal event}}
          {{> home/card blog}}
        </div>
      </div>
    </div>
  </div>
</div>
