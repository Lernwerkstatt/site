<article class="pb-4 mb-4">
  <img src="{{ imagelink }}" alt="{{ title }}" class="img-fluid w-100 mb-4">
  <div class="px-4">
    <h1>{{ title }}</h1>
    <p class="text-muted"><small>{{ author }}, {{ date }}</small></p>
    {{{ content }}}
  </div>
</article>
