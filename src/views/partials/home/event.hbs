<li class="list-group-item">
  <a href="#" data-toggle="modal" data-target="#name">
    <p class="lead mb-0">
      <img src="{{ dayicon }}" />
      {{ name }}
    </p>
    <small class="text-muted">{{ date }} - {{ time }}</small>
  </a>
  {{#each name}}
  {{>modal this}}
  {{/each}}
</li>
