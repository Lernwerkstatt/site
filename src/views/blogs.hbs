<div class="container-fluid text-center">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-10 col-lg-6 mt-4">
      {{#if blogs}}
      <h1 class="mt-3">Der Blog</h1>
      <h5>übers Lernen. Was sonst?</h5>
      {{#each blogs}}
      {{> blogs/posts}}
      {{/each}}
      {{else}}
      {{> blogs/single this}}
      {{/if}}
    </div>
  </div>
</div>
