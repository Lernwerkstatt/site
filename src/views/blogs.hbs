{{> main/title title="Der Blog"}}
<div class="container-fluid text-center">
  <div class="row justify-content-center">
    <div class="col-sm-12 col-md-10 col-lg-6 mt-4">
      {{#if blogs}}
      <h2>Übers Lernen. Was sonst?</h2>
      {{#each blogs}}
      {{> blogs/posts}}
      {{/each}}
      {{else}}
      {{> blogs/single this}}
      {{/if}}
    </div>
  </div>
</div>
