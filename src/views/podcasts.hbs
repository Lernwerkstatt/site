{{#section "head"}}
<title>{{{__ "podcasts_title"}}} – {{{__ "common_title"}}}</title>
{{/section}}

{{#section "title"}}
<div class="container my-4">
  <div class="row">
    <div class="col border-bottom">
      <h1 class="h1">{{{__ "podcasts_title"}}}</h1>
    </div>
  </div>
</div>
{{/section}}

<div class="container mb-4">
  <p>{{{__ "podcasts_text"}}}</p>
  <div class="row">
    {{#if podcasts}}
    {{#each podcasts}}
    {{> podcasts/posts}}
    {{/each}}
    {{else}}
    {{> podcasts/single this}}
    {{/if}}
  </div>
</div>
